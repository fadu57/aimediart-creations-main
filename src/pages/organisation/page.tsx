/**
 * Équivalent App Router Next.js : `app/organisation/page.tsx`
 *
 * Ce projet utilise Vite + React Router. La route `/organisation` est déclarée dans
 * `src/App.tsx` et le prérendu SEO est généré au build via `scripts/prerender-organisation.tsx`.
 */
export { default, type PublicHomeProps } from "../PublicHome";
